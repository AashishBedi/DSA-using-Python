import sys
sys.set_int_max_str_digits(0)
class Solution:
    def addStrings(self, num1: str, num2: str) -> str:
        n1 = int(num1)
        n2 = int(num2)
        s1 = n1 + n2
        return str(s1)
