class Solution:
    def isAnagram(self, s: str, t: str) -> bool:
        '''
#Way 1 to solve it
        s= sorted(s)
        t= sorted(t)

        return s == t
        '''
#Way 2 to solve  it
        counter_s = Counter(s)
        counter_t = Counter(t)

        return counter_s == counter_t
