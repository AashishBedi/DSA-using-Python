class Solution:
    def addDigits(self, num: int) -> int:
        while num > 9:
            Sum = 0
            while num:
                Sum += num%10
                num = num//10
            num = Sum
        return num
