class Solution:
    def majorityElement(self, nums: List[int]) -> int:
        nums.sort()
        return nums[len(nums)//2]

        '''
        count = 0
        for i in range(len(nums)):
            if i in nums:
                count += 1
            
        return count
        '''
