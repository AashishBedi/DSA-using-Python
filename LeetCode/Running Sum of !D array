class Solution:
    def runningSum(self, nums: List[int]) -> List[int]:
        for i in range(1, len(nums)):
            nums[i] += nums[i-1]
        return nums
'''
            ls.append(nums[i]+nums[i+1])
            i += 1
'''
           # ls.append(nums[i]**2)
        #return ls
